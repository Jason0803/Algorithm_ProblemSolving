#include <iostream>
#define MAX_SIZE 100
using namespace std;
int n, r, c, turn;
char map[MAX_SIZE][MAX_SIZE];
int main() {
    cin >> n;
    while(true) {
        // r과 c는 배열 map 범위 안의 값만 들어오고, 이미 놓인곳 안들어온다고 가정.
        turn
        cin >> r >> c;
        
    }
    
    
    return 0;
}
