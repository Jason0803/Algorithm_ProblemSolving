#include <cstdio>
#include <set>

int main() {
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    set<int> s;
    bool check[201][201];
    
    s.insert(c);
    
    int x, y, z;
    x = 0;
    y = 0;
    z = c;
    while(true){
        
    }
    return 0;
}
