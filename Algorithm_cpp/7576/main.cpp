#include <cstdio>
#include <algorithm>
#include <vector>
using namespace std;

int N, M;
int box[1000][1000];
int visited[1000][1000];
int dist[1000][1000];
int dr[] = {0, 0, 1, -1};
int dc[] = {1, -1, 0, 0};


int main() {

    scanf("%d %d", &M, &N);
    for(int i = 0; i < N; i++) {
        for(int j = 0; j < M; j++) {
            scanf("%1d", )
        }
    }
    return 0;
}
