#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define RANGE 1000001

int countWords(char* input) {
	int count = 0;
	int index = 0;
	char** words = (char** )malloc(sizeof(char* ) * strlen(input));
	char* word = "";
	for(int i = 0; i < strlen(input); ) {
		index = strchr(subs, )
		memcpy(word, substr(i, ) )
	}

	return count;
}

int main() {
	char input[RANGE];
	int count = 0;
	//memset(input, '\0', RANGE);

	fgets(input, RANGE, stdin);


	printf("%d", count);
}